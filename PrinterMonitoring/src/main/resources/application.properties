spring.application.name=PrinterMonitoring

# Connexion à la base PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/printerdb
spring.datasource.username=postgres
spring.datasource.password=123456789
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuration JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration (optionnel)
server.port=8080

# JWT configuration - IMPORTANT: Change this secret key!
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# 24 hours for access token (Moved comment to its own line)
jwt.expiration=86400000

# 7 days for refresh token (example value) (Moved comment to its own line)
jwt.refresh.expiration=604800000

# Logging Configuration
logging.level.com.snmp.PrinterMonitoring=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%level] %logger{36} - %msg%n

# Jackson Configuration
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.default-property-inclusion=NON_NULL

# Connection Pool Configuration (pour PostgreSQL)
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5